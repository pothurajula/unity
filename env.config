export GIT_LFS_SKIP_SMUDGE=1

echo -n "Please input your password ->"

read -s input_passwd

printf "\n"

git config --global credential.helper cache

git config --global user.name <user>

git credential-cache --timeout=5000 store <<- __EOF__

protocol=https

host=eos2git.cec.lab.emc.com

username=<user>

password=$input_passwd

__EOF__

git credential-cache --timeout=5000 store <<- __EOF__

protocol=https

host=amaas-mr-mw1.cec.lab.emc.com

username=<user>

password=$input_passwd

__EOF__